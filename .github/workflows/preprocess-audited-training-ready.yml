name: Preprocess audited training-ready corpus once

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'One-shot preprocessing completed and is archived.'

name: Legacy PDMX orchestra selector (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Explain disabled workflow
        run: |
          echo 'This legacy selector previously made an unsupported 10,000-orchestra claim.'
          echo 'It is intentionally disabled. Use the audited manifests and field-specific validators instead.'
          exit 1

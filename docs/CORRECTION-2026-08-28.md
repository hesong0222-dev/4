# Correction log — 2026-08-28

The following statements were removed because the generated artifacts did not support them:

- “10,000 unique orchestral full-score records”
- “strict band pairs were collected”
- “the latest main-branch 10k verifier passed”

The preserved successful artifact establishes only 17,599 **unclassified multi-instrument exact score-to-synthesis pairs**. Its own summary reports zero strict-band and zero broad-band rows. Future counts must be produced from committed manifests and independent validators.
